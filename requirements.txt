# ============================================================
#  MATH-86 Quanto Project — Python dependencies
# ============================================================

# --- Core data stack -----------------------------------------
numpy>=1.26
pandas>=2.2
openpyxl>=3.1          # Read Bloomberg-exported .xlsx files (loader.py reference code)

# --- Visualisation -------------------------------------------
matplotlib>=3.8

# --- Testing -------------------------------------------------
pytest>=8.0

# --- Bloomberg Desktop API (optional — only needed for re-pulling data) -----
# Pre-installed via Bloomberg API SDK on Bloomberg terminals. Not on PyPI.
# Only required to run scripts/bdh_*.py or tests/test_bbg_connection.py.
# Do NOT pip install this — it comes with the Bloomberg terminal software.
# blpapi
seaborn>=0.13          # Optional: higher-level statistical plots

# --- Development / testing -----------------------------------
pytest>=8.0
pytest-cov>=5.0
